{
  "checks": {
    "sshd_check": {
      "handlers": ["default"],
      "command": "ruby /etc/sensu/plugins/check-procs.rb -p sshd -C 1 ",
      "interval": 60,
      "subscribers": [ "webservers" ]
    }
  }
}
